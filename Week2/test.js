var RandomizedQueue = Java.type("RandomizedQueue");
var r = new RandomizedQueue;

r.enqueue("1");
r.enqueue("2");
r.enqueue("3");
r.enqueue("4");
r.enqueue("5");

for each (var e in r) print (e);
for each (var e in r) print (e);
for each (var e in r) print (e);
for each (var e in r) print (e);
for each (var e in r) print (e);


